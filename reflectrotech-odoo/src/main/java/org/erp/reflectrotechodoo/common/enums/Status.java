package org.erp.reflectrotechodoo.common.enums;

/**
 * Universal status enum used across all ERP entities.
 * Prevents magic strings and ensures consistent status management.
 */
public enum Status {
    ACTIVE,
    INACTIVE,
    BLOCKED,
    PENDING,
    ARCHIVED
}
